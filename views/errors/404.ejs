<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head', { title: '404 | ' + (site?.name || 'Asociación Privada de Patronato'), extraCss: [] }) %>
<body>
  <%- include('../partials/navbar') %>

  <main class="container py-5" style="min-height: 60vh;">
    <div class="row justify-content-center">
      <div class="col-12 col-lg-8 text-center">
        <h1 class="fw-bold mb-3">Página no encontrada</h1>
        <p class="lead text-muted mb-4">
          La página que buscas no existe o fue movida.
        </p>
        <div class="d-flex gap-3 justify-content-center flex-wrap">
          <a class="btn btn-dark px-4" href="/">Ir al inicio</a>
          <a class="btn btn-outline-dark px-4" href="/contacto">Contacto</a>
        </div>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>
  <%- include('../partials/scripts') %>
</body>
</html>
